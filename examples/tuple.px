main := fn () -> i32 {
  // Tuples
  let tuple: (i32, i32) = (0, 1);

  let v1, v2 := tuple;
  var v1_1, v2_1 := tuple;
  (let v1_2, var v2_2) := tuple;


  let named := (health: 100, alive: true);
  let (:health, :alive) := named;
  let (health: h, alive: a) := named;

  v2_1
}



